let baseURL = '';

// if ( process.env.NODE_ENV === 'development' ) {
// 	// baseURL = ''; //王强本地测试
// 	baseURL = ''; // 测试环境
// } else {
// 	baseURL = '' // 线上环境
// }

export default baseURL
